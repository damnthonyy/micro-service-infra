name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  generate-and-lint-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: 📦 Install dependencies
        run: |
          cd backend
          for i in 1 2 3; do
            echo "Attempt $i to install dependencies..."
            if npm ci --prefer-offline --no-audit; then
              echo "✅ Dependencies installed successfully"
              break
            elif [ $i -eq 3 ]; then
              echo "⚠️ npm ci failed, trying npm install as fallback..."
              npm install --prefer-offline --no-audit || exit 1
            else
              echo "⏳ Retrying in 5 seconds..."
              sleep 5
            fi
          done
          cd ..

      - name: 🔧 Create .env file
        run: |
          cat > .env << EOF
          POSTGRES_USER=app
          POSTGRES_PASSWORD=app
          POSTGRES_DB=appdb
          POSTGRES_PORT=5432
          API_PORT=3000
          DB_HOST=localhost
          DB_PORT=5432
          DB_USER=app
          DB_PASS=app
          DB_NAME=appdb
          EOF

      - name: 🐳 Start Docker Compose services
        run: |
          docker compose up -d db-microservice-infra-boilerplate
          echo "⏳ Waiting for database to be ready..."
          for i in {1..30}; do
            if docker compose exec -T db-microservice-infra-boilerplate pg_isready -U app -d appdb > /dev/null 2>&1; then
              echo "✅ Database is ready (attempt $i)"
              break
            fi
            echo "Waiting for database... (attempt $i/30)"
            sleep 2
          done
          echo "⏳ Waiting additional time for database to fully initialize..."
          sleep 10
          echo "✅ Verifying database is accessible from host..."
          docker compose ps
          echo "Testing port 5432 accessibility..."
          timeout 5 bash -c 'until nc -z localhost 5432; do sleep 1; done' && echo "✅ Port 5432 is accessible" || echo "⚠️ Port test failed, but continuing..."
          echo "✅ Database should be ready"

      - name: 🏗️ Build & Export OpenAPI
        run: |
          cd backend
          npm run build
          echo "🚀 Starting backend to generate OpenAPI..."
          echo "⏳ Waiting a bit more before starting backend..."
          sleep 3
          NODE_ENV=development \
          DB_HOST=localhost \
          DB_PORT=5432 \
          DB_USER=app \
          DB_PASS=app \
          DB_NAME=appdb \
          API_PORT=3000 \
          node dist/main.js > backend.log 2>&1 &
          BACKEND_PID=$!
          echo "Backend PID: $BACKEND_PID"
          echo "⏳ Waiting for backend to connect and generate OpenAPI..."
          sleep 5
          for i in {1..40}; do
            if [ -f openapi.yaml ] && [ -s openapi.yaml ] && grep -q "openapi: 3.0.0" openapi.yaml; then
              echo "✅ OpenAPI file generated successfully"
              break
            fi
            if grep -q "Unable to connect to the database" backend.log && [ $i -gt 20 ]; then
              echo "❌ Database connection failed after 20 attempts"
              cat backend.log
              exit 1
            fi
            sleep 1
          done
          sleep 3
          if [ -f openapi.yaml ] && [ -s openapi.yaml ]; then
            echo "✅ openapi.yaml generated successfully"
            head -20 openapi.yaml
            mv openapi.yaml ../openapi.yaml
          else
            echo "❌ Failed to generate openapi.yaml"
            echo "Backend logs:"
            cat backend.log || true
            echo "Files in backend directory:"
            ls -la || true
            exit 1
          fi
          kill $BACKEND_PID 2>/dev/null || true
          wait $BACKEND_PID 2>/dev/null || true
          cd ..

      - name: 🔍 Install Spectral
        run: |
          npm i -g @stoplight/spectral-cli --prefer-offline --no-audit || \
          (sleep 5 && npm i -g @stoplight/spectral-cli --prefer-offline --no-audit) || \
          (sleep 10 && npm i -g @stoplight/spectral-cli --prefer-offline --no-audit)

      - name: 🧪 Lint OpenAPI spec
        run: |
          if [ ! -f openapi.yaml ] || [ ! -s openapi.yaml ]; then
            echo "❌ openapi.yaml is missing or empty"
            exit 1
          fi
          spectral lint openapi.yaml -f json -o spectral-report.json || true

      - name: 📤 Upload lint report
        uses: actions/upload-artifact@v4
        with:
          name: spectral-report
          path: spectral-report.json

